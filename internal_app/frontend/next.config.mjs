/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  basePath: "/navi",
  assetPrefix: "/navi/",
  reactStrictMode: true,
};

export default nextConfig;
